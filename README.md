# OraWigs
Website for Ora wigs company

Installation requirements:
pip install json
pip install bs4
pip install requests 
pip intall sqlite3 (and add sqlite3 viewer to see database in codespace)

To start run createDB.py so the databse is created. 
then to run the streamlit app do this command to run app:
streamlit run OraWigs_app.py

